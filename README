The implementations are made to run in google Collab enviroment. 
Using these implementations is simple and most only need to have their cells run from top to bottom to produce results.
With the exception of CapsNet-Visualization, that needs to be run according to its readme file.


Important Google Collab 


All linux commands work within the enviroment 


Downloading a file:

from google.colab import files
files.download( "model.png" )


Uploading a file:

from google.colab import files
uploaded = files.upload()



